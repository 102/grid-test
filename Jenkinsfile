pipeline {
  agent any
  tools {
    nodejs 'stable'
  }
  stages {
    stage('') {
      steps {
        sh 'echo "Testing"'
        sh 'node --version'
        sh 'npm --version'
      }
    }
  }
}
